spring:
  application:
    name: spring-security-6
  security:
    user:
      name: manu
      password: password

logging:
  level:
    com.dailycodebuffer.security: DEBUG
    org:
      springframework:
        security: TRACE
        web:
          csrf: TRACE
          header: TRACE
        access: DEBUG